/**
 * Outras coisas que são operadores
 */

/// Operador de resolução de escopo (::)
namespace Espaco1 {
    int valor = 42;
}
int valor1 = Espaco1::valor; // 42

/// Operador de chamada de função (())
int funcao(int x) {}
void main(){}

/// Operador de vírgula (,)
int a = (1, 2, 3); 
// a será 3, pois o operador de vírgula avalia da esquerda para a direita e retorna o último valor

/**
 * Operadores de ponteiro para acesso a membro direto eu vou deixar para aulas mais avançadas!
 * (.*) e (->*)
 */

 