const char* nome = "MinervaBots";

int numeros[6] = {0, 1, 2, 3, 4, 5};

int *ptrnull = NULL;

int a = 10;

int &refA = a; // refA é uma referência para a variável a (vai armazenar o endereço físico de a)


int *ptr = nullptr; // não aponta para nada, em algum momento ele pode apontar para um inteiro
auto *ptrAuto = nullptr // pode apontar para qualquer coisa em qualquer momento